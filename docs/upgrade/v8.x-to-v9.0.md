# Upgrading v8.x → v9.0

## Breaking change: HTTP long-poll is now the default

Workers now receive work through HTTP long-polling instead of the deprecated MQTT transport. The module exports:

```sh
SPACELIFT_WORKER_COMMS_PROTOCOL=poll
SPACELIFT_WORKER_COMMS_URL=https://app.spacelift.io
```

The change takes effect when an EC2 worker starts with launch-template user data generated by v9.0.

## Action required

Before upgrading:

1. Allow workers to make outbound HTTPS connections on port 443 to their Spacelift server.
2. For US-region SaaS, set `domain_name = "us.spacelift.io"` so the communication URL is `https://app.us.spacelift.io`.
3. For self-hosted and FedRAMP Spacelift, set `worker_comms_url` to the base URL provided for the environment.
4. Remove MQTT or AWS IoT egress rules only after all workers have been replaced and are successfully using long-polling.

If you cannot migrate immediately, retain MQTT temporarily:

```hcl
worker_comms_protocol = "mqtt"
```

MQTT worker communication is scheduled to be retired at the end of 2026, so the opt-out is intended only as a migration fallback.

User-provided `configuration` is applied after these module defaults and can still override the exported values, but using `worker_comms_protocol` and `worker_comms_url` is recommended.
